"use client";
import React from "react";
import * as _Builtin from "./_Builtin";

export function BenchmarkCharts({ as: _Component = _Builtin.NotSupported }) {
  const _styleVariantMap = {
    all: "w-variant-all",
    range: "w-variant-range",
  };

  const _activeStyleVariant = _styleVariantMap[initialRevenueSetup];
  return <_Component _atom="CodeIsland" />;
}
