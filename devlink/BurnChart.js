"use client";
import React from "react";
import * as _Builtin from "./_Builtin";

export function BurnChart({ as: _Component = _Builtin.NotSupported }) {
  const _styleVariantMap = {
    multiple: "w-variant-multiple",
    "rule-of-40": "w-variant-rule-of-40",
    "operating-margin": "w-variant-operating-margin",
  };

  const _activeStyleVariant = _styleVariantMap[initialMetric];
  return <_Component _atom="CodeIsland" />;
}
